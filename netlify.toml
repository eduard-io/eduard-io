[build]
  command = "npm run build"
  publish = "dist"

[build.environment]
  NODE_VERSION = "24.8.0"

# CONTEXT is supplied by Netlify. Preview indexing headers are generated
# by astro.config.mjs; analytics are included only in production builds.
